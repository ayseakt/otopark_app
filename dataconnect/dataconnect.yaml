specVersion: "v1"
serviceId: "otoparkapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "otoparkapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
